import * as React from 'react';

/**
 * if the url is not valid, show this page.  contains no other components
 * @returns {JSX.Element}
 * @constructor
 */
export default function NoPage() {

    return (
            <h1>Sorry, something went wrong...</h1>
    );
}
